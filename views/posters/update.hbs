{{#extends 'base'}}

{{#block 'content'}}
  <h1>Update poster: {{poster.title}}</h1>
  <form method="post">
    {{!-- Render form with TRIPLE BRACES --}}
    {{{form}}}

    {{!-- CSRF --}}
    <input type="hidden" name="_csrf" value="{{csrfToken}}">
    
    <input type="submit" value="Update" class="btn btn-primary">
  </form>
{{/block}}

{{/extends}}